name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Inject Telegram token into payment.js
        run: |
          sed -i "s|let BOT_TOKEN = '';|let BOT_TOKEN = '${{ secrets.TELEGRAM_BOT_TOKEN }}';|" assets/js/payment.js
          sed -i "s|let ADMIN_CHAT_ID = '';|let ADMIN_CHAT_ID = '${{ secrets.TELEGRAM_CHAT_ID }}';|" assets/js/payment.js

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4